<?php

/**
 * Write your own helper functions here
 * 
 * Custom functions, constants, etc. or inherit Click from $driver and 
 * use it with more flexibility
 * 
 * eg. 
   function click($driver, $element) {
    $driver->wait(20)->until(
        WebDriverExpectedCondition::elementToBeClickable($element)
    );
    $element->click();
   }
 */